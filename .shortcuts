
ssh-add ~/.ssh/ansible
ssh-add ~/.ssh/azure_rsa

set_vault() {
  export VAULT_ADDR='https://inno-vault.eagleinvsys.com'
  echo "Enter Vault Token: "
  read -s VAULT_TOKEN
  export VAULT_TOKEN=${VAULT_TOKEN:-NoTokenSet}
}

# needs key name, user, host
ssh_other()
{
  ssh -i ~/.ssh/${1} ${2}@${3}
}

ssh-ans()
{
  ssh_other "ansible" "ansible" ${1}
}

ssh_az()
{
  ssh_other "azure_rsa" "ansible" ${1}
}
